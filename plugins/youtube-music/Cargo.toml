[package]
name = "auqw-youtube-music"
version = "0.2.0"
edition = "2021"
license = "GPL-3.0-only"
description = "YouTube Music playback.resolve + playback.candidates guest (ABI 0.2.0)"

[lib]
crate-type = ["cdylib", "rlib"]

[lints]
workspace = true

[dependencies]
auqw-guest-sdk = { path = "../../vendor/auqw-guest-sdk-0.3.0" }
serde_json = "1.0.149"
base64 = "0.22.1"
